services:
  web:
    build:
      context: .
      target: dev
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      # keep node_modules from being overwritten by the bind mount
      - /app/node_modules
    restart: unless-stopped


